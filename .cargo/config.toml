[target.'cfg(target_arch = "x86_64")']
# Give the interpreter roundsd & friends: without SSE4.1, f64::floor() in
# the MOD opcodes compiles to a libm call inside the dispatch loop.
rustflags = ["-C", "target-cpu=x86-64-v2"]
